# This repo publishes docs/ (GitHub Pages source). tests/, examples/, and
# documentation/ are also tracked now that their contents have been checked
# over for PII and anonymized (see CLAUDE.md's PII rule) — keep it that way
# for anything added under them going forward.
*
!docs/
!docs/**
!examples/
!examples/**
!documentation/
!documentation/**
!test/
!test/**
!package.json
!package-lock.json
!.gitignore
!CLAUDE.md

# OS cruft
.DS_Store

# Local machine-specific tool settings, dependency artifacts
node_modules/
